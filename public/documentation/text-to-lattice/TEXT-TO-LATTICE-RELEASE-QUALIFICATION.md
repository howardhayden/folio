# Text to Lattice release qualification

**Decision:** qualify and enable the interactive client as a bounded demonstrable release.

**Reviewed baseline:** `233bdbc8dbafd401a94babe14a64c198be110830`

**Qualification date:** 2026-09-09

**Machine authority:** [`TEXT-TO-LATTICE-RELEASE-REGISTER.json`](TEXT-TO-LATTICE-RELEASE-REGISTER.json)
**Qualified source-set SHA-256:** `6f118d0c753e2d23edeaaf39674abfb060104a4aea294f090c710551856083d1`
**Artifact-set projection SHA-256:** `5613bf89d45e9b61213b6cc4c2bd6d4a72d2e4e4d3bcda34d0fcf97a587eaf1f`

This is an engineering release decision, not legal advice, an upstream warranty, or independent certification. It applies only to the exact revisions and digests named below. A source test proves a source contract; it does not prove a production origin, provider setting, assistive-technology path, network trace, third-party license grant, or reproducible binary.

## Why the client is qualified

GATE-02 is satisfied in production. The canonical [workflow run URL](https://github.com/howardhayden/folio/actions/runs/34320931448) records `service job 102367490246`, `commit a9b1db818dc7bb87b5bee16d0e168bf17d130185`, `live verification at 2026-09-09T06:52:40.6740509Z`, and current-main postcheck at `2026-09-09T06:52:41.0031258Z`. The successful job deployed `response-policy Worker version 06e29e21-bbba-48d7-9fe3-28e8a960a88f`, `verification-frame Worker version 8c7d9e11-f6b8-40b2-9b5d-b9e2af082271`, and `lease Worker version 1a18b449-2341-449b-96e1-f27451efd483`. Its exact route inventory covered the four protected `hah.dev` document routes, `verify.hah.dev`, and the independent lease route with no missing, extra, or stale owned routes; it verified frame bytes and isolation headers, main-page response policies, and the four encrypted binding-name inventory without reading values. Its fail-closed probes covered method, cross-origin, cookie, missing-attestation, and invalid-attestation behavior, including first-visitor and returning-visitor flows. The exact official dummy-token lifecycle returned `HTTP 200 acquisition` and `authenticated bodyless HTTP 204 release`. This direct probe establishes only the deployed Siteverify, lease, and release path; it does not prove frame or widget participation, canonical-browser GATE-06, human verification, or production anti-bot assurance. This is high-marginal-value live evidence for an ordinary, consequential dependency of the public interaction.

The earlier GATE-06 lifecycle and privacy record remains historical evidence for its deployed revision. It came from the activated canonical client at `https://hah.dev/resume/#text-to-lattice`, after [workflow run 34325228788](https://github.com/howardhayden/folio/actions/runs/34325228788) deployed `commit 9ab26b95cc1f9a94697118c0fc20a849db8f6ad2`. Safari Version 26.5 (21624.2.5.11.4), WebKit, on macOS 26.5.1 (25F80), M5 iMac, directly recorded `POST 428 → isolated verify.hah.dev frame and challenge resources → POST 200 → DELETE 204`, including a zero-byte final response, at the operator-normalized capture time `2026-09-09T08:05:50Z`. Brave 1.94.121 (Chromium 152.0.7977.83, arm64) on the same platform independently recorded the second-engine lease statuses `428 → 200 → 204`, both origins, and pinned Qwen tokenizer and model-asset loading during the operator-normalized window `2026-09-09T08:15:12Z` through `2026-09-09T08:22:30Z`; the deployed canonical-client source correlates those ordered rows to `POST`, `POST`, and `DELETE`, while the screenshots do not independently prove the later request methods. The owner reports that a capture-wide search for the unique submitted source marker returned `0 matches` across the Brave network record. That search establishes the source-specific negative check. Deployed source contracts and tests keep source, clarification, candidate, verifier finding, and output browser-local and exclude them from lease, attestation, model-asset, error, or telemetry traffic; the captured inventory shows no undocumented content-bearing route. The canonical URL, bodyless request and release source contracts, and exact official testing profile come from the deployed source and operator record, not from a claim that screenshot pixels prove an inspected Payload pane. The UTC times normalize the local capture filenames and are not server-signed timestamps.

Cloudflare automatically edge-injected a Web Analytics tag for `https://static.cloudflareinsights.com/beacon.min.js` into both document responses. Each CSP blocked the external beacon resource before transfer or execution (`blocked:csp`, `0.0 kB`), and the trace contains no `/cdn-cgi/rum` or other RUM transmission. This is a retained provider-mutation residual, not evidence that Web Analytics is disabled or that Cloudflare receives no ordinary edge metadata. The reviewed evidence establishes demonstrator integration and the observed browser-local privacy boundary under the official testing profile; it does not establish human verification, bot resistance, token freshness or single use, hostname or action assurance, or universal browser behavior.

The post-capture delta now includes a build-time model-worker realm repair. It changes emitted runtime behavior without changing the pinned model, tokenizer, WASM, lease, attestation, origin, provider, telemetry, or request-contract identities. A worker-only Vite pre-transform preserves fourteen bare realm checks across the pinned WebLLM and web-tokenizers modules with fail-closed twelve/two count assertions; the final-artifact smoke boots the emitted bundle without window, completes the cached RPC, reaches the first approved generator model-configuration cache request, and makes no network request. The earlier trace remains historical evidence for its deployed revision and did not demonstrate a terminal conversion. GATE-06 is therefore post-deployment verification until the repaired official page records one supported-browser non-error terminal conversion and repeats the lifecycle and sanitized two-origin privacy trace.

The other gaps are real, but they do not all have the same release consequence:

- Exact release identity is **release-workflow-enforced**. The qualified source set binds the activation, runtime, validator, complete test tree, license-routing map, shipped legal notices, and workflow sources without hashing this register or generated evidence. Current-main checks separately bind the deployed commit. This records a mandatory lifecycle control; it does not claim that a future workflow run has already happened.
- Capacity remains **post-deployment verification**, and GATE-06 has returned to **post-deployment verification** for the repaired model-worker runtime. The dated Safari/WebKit and Brave/Chromium evidence remains historical lease-lifecycle and privacy evidence for its deployed revision, but it did not demonstrate a terminal conversion. The direct GATE-02 token lifecycle remains server evidence rather than browser or privacy evidence. The repaired deployment must add one supported-browser non-error terminal conversion and repeat the canonical-client lifecycle and sanitized two-origin privacy trace, with a condition that returns the client to held documentation-only publication on failure.
- Exact-model Llama fixture execution, the incomplete converted-model custody narrative, the absent historical WASM rebuild, and the eventual complete assistive-technology matrix are **accepted residual risks**. Their evidence gaps stay explicit. A verifier false negative is not bounded merely because explicit denials are withheld, and artifact hashes do not establish license or notice custody. Source confirmation, tool-free local operation, human review, exact technical identity, accessibility contracts, and requalification triggers bound the official product surface without pretending the missing evidence exists.

This is not a relaxation into “edge cases do not matter.” It is a classification by consequence × plausibility × lifecycle value. A rare finding can still block release when its realistic consequence is high. Conversely, byte-for-byte reconstruction of an undocumented workstation WASM build has negative marginal value for this product boundary after exact artifact identity, lineage, ABI, configuration, release-time digest checks, and runtime integrity checks are bound. The exact two WASM and two tokenizer requests enforce their recorded SRI values; protected Cache API hits are SHA-256 checked and mismatches are evicted before use, while allowed model shards remain bounded to immutable model-revision URLs. Upstream clarification or a maintainable controlled replacement remains useful; bespoke historical reconstruction is not a condition of publication.

Enabling the client does not erase its evidence boundaries. The interaction, canonical project page, mapped documentation, source, and qualification evidence are published together, while the machine rule remains direct: the public client is enabled if and only if no gate is `open-release-blocker`.

The evidence order remains important. GATE-02 can and must establish the deployed response policy, frame bytes and headers, lease route, exact Worker route inventory, bindings, separated secret names, declared credential profile, every checked-in live noninteractive or intentionally invalid-token result, and one direct exact-dummy-token 200 acquisition plus 204 cleanup before activation; run 34320931448 supplied that evidence. That direct request proves only the deployed Siteverify, lease, and release path. The earlier GATE-06 first-session canonical-client evidence independently records the real 200 acquisition, 204 release, supported Safari/WebKit and Brave/Chromium engines, and sanitized two-origin privacy trace, but the rapid cleanup did not prove a successful conversion. GATE-06 is now **post-deployment verification**: the repaired canonical client must record one supported-browser non-error terminal conversion plus the repeated lifecycle and privacy trace. Under Cloudflare's official testing pair, those browser requests establish demonstrator integration rather than production anti-bot assurance. A real renewal remains valuable follow-up evidence when an ordinary session naturally reaches the interval, but deliberately waiting to record a renewal is not a condition of operational completion. The machine rollback sets GATE-06 itself to open-release-blocker if the repaired runtime, a repeated mandatory step, privacy trace, or observed real renewal fails. A separate public or operator bypass harness remains outside the authorized boundary.

## Bounded Cloudflare demonstration profile

The bounded profile uses Cloudflare's exact published Turnstile testing site and secret pair, named in `workers/text-to-lattice-lease/demonstrationProfile.js`. The values are intentionally public and deterministic. The official canonical browser flow can exercise the isolated frame, widget, exact published dummy token, server-side Siteverify call, lease, and release. The direct GATE-02 probe sends the exact dummy token without a browser widget and therefore establishes only the deployed Siteverify, lease, and release path. In either case the dummy token is reusable: the profile does not establish that a visitor is human, prove one-time token use, prove frame or widget participation from a server result, restrict a widget to `verify.hah.dev`, or provide production anti-bot protection. The release must say “demonstration profile,” not silently inherit the stronger claims associated with a real hostname-restricted widget.

This is narrower than a project-defined bypass. Only the exact provider-published testing secret activates the testing branch, and only the exact provider-published dummy token is accepted there. The returned Siteverify response still has a bounded age, but the dummy token itself is not fresh or single-use and its returned hostname and action do not establish origin or purpose. Exact parent/frame origins and window identity, the closed message schema, bodyless acquisition, independent cookie and lease signing domains, per-location shaping, global minute and daily admission, the eight-lease cap, bounded expiry, Siteverify failure handling, and browser-local prose boundary remain intact. An unsigned assertion, alternate public route, operator-only page, or second verification protocol would add attack and maintenance surface and remains outside the authorized product boundary.

Those controls bound financial, privacy, and state consequence; they do not make the demonstrator reliably available. With the public reusable dummy token, one client can complete the permitted 16-request acquisition sequence, fill all eight slots, and renew them. The proposed 48-request/10-second per-IP edge rule admits that sequence and therefore cannot be represented as prevention. The resulting availability gap is moderate accepted residual risk for this bounded portfolio demonstrator, whose interface promises no availability assurance. The edge rule remains useful operational defense in depth under GATE-03, and observed slot abuse triggers requalification.

A complete existing Turnstile pair is preserved without reading or replacing its values. The current workflow then requires the exact official demonstration site key and a successful exact dummy-token lifecycle, so a preserved real or otherwise different pair fails this demonstrable profile's verifier rather than being silently relabeled. Moving to a real hostname-restricted pair requires an intentional workflow-verifier and register change plus repeated live qualification; the production code path then restores hostname and `text_to_lattice` action checks. The release may be described as operationally anti-bot protected only after that real profile and its first-session evidence exist. Until then, the official testing pair is a moderate, explicitly accepted reduction in bot and availability assurance for a capacity-capped portfolio demonstrator—not evidence that anti-bot protection was tested.

## Exact response policy and bounded bootstrap

GitHub Pages cannot emit the required response policies itself. The small Worker at `workers/text-to-lattice-response-policy/worker.js` is therefore part of the qualified source set. It is attached only to the four exact Cloudflare routes `/`, `/index.html`, `/resume/`, and `/resume/index.html`; it is not attached to `hah.dev/*`. The canonical Text to Lattice launch is `https://hah.dev/resume/#text-to-lattice`; its fragment never reaches the server, so the request remains on the protected `/resume/` document route. The document includes the same CSP as an in-document fallback for legacy query-state or origin-preview contexts. The HTTP response remains authoritative on the four supported public aliases and uniquely supplies `Permissions-Policy: document-domain=()`. Unrelated pages, assets, and arbitrary paths do not invoke this Worker, while the lease route remains independently owned. It has no bindings, storage, telemetry, content inspection, preview URL, or observability. Run 34320931448 verified all four aliases, their response policies, the independent route inventory, and the unrelated-traffic boundary in production.

An enabled release runs the protected service job on every push to `main`. In a held state, service mutation is explicit: the job runs only for a main-branch commit whose head message contains `[deploy-text-to-lattice-services]`, or for an approved manual dispatch with `deploy_text_to_lattice_services`. The job deploys the response-policy Worker; deploys and byte/header-verifies the isolated frame; deploys the lease Worker; reads only encrypted binding names; bootstraps missing bindings; verifies the exact four-name secret inventory; reads the authenticated zone route inventory and rejects any missing, extra, or stale wildcard route owned by either hah.dev Text to Lattice Worker; runs live boundary probes; and checks current-main freshness again. Enabled publication requires that service job to succeed for every main-branch release.

Bootstrap is preservation-first. A complete existing binding set is not changed. Missing cookie and lease signing values are independently generated from 48 random bytes. If and only if both Turnstile bindings are absent, the workflow's explicit `--require-official-test-profile` option adds the exact provider-published testing pair. All missing bindings are sent through one Wrangler `secret bulk` operation, avoiding the partially installed state created by sequential writes. A one-sided Turnstile pair, unexpected binding name, invalid inventory, ambiguous bulk plan, installation failure, or live-probe failure stops the job rather than mixing profiles or overwriting values. The later site-key and direct dummy-token probes ensure a preserved pair is actually the declared demonstration profile; a different complete pair stays untouched and fails closed until an intentional profile change. A requested held service job must succeed before that commit's Pages publication; enabled publication always requires the same service job to succeed.

The protected deploy token is least-privilege but must cover Account → Workers Scripts → Edit, Zone → Workers Routes → Edit, and Zone → Zone → Read for the hah.dev account and zone. Zone read is required only to resolve the active zone before the exact route inventory is inspected; it does not grant DNS or content mutation.

## Owner direction and evidence boundary

Owner direction authorizes inclusion after the open release blocker closes; it is not deployment or runtime evidence. The activation condition was met by the separately recorded GATE-02 workflow evidence, not by owner direction alone. The earlier GATE-06 canonical-browser lifecycle and privacy record remains historical for its deployed revision; the repaired runtime requires repeated supported-browser verification and one non-error terminal conversion. The direction is bound to the qualified source-set digest, not to the older implementation baseline alone. It does not manufacture provider-capacity, broader exact-model-fixture, assistive-technology, artifact-license, or reproducible-build evidence.

## Evidence register

| Boundary | Exact evidence | What it establishes | What it does not establish |
| --- | --- | --- | --- |
| Implementation | [`233bdbc8dbafd401a94babe14a64c198be110830`](https://github.com/howardhayden/folio/commit/233bdbc8dbafd401a94babe14a64c198be110830) | Reviewed source baseline | Later changes or built output |
| GATE-02 production qualification | [Workflow run 34320931448](https://github.com/howardhayden/folio/actions/runs/34320931448), `service job 102367490246`, `commit a9b1db818dc7bb87b5bee16d0e168bf17d130185`, `live verification at 2026-09-09T06:52:40.6740509Z`, current-main postcheck `2026-09-09T06:52:41.0031258Z`; response-policy, verification-frame, and lease Worker versions named above | Exact service deployment, route and encrypted-binding-name inventory, frame bytes and headers, response policies, fail-closed method/origin/cookie/attestation probes, and direct official dummy-token `HTTP 200 acquisition` plus `authenticated bodyless HTTP 204 release` | Frame or widget participation, canonical-browser lifecycle, privacy trace, human verification, production anti-bot assurance, provider-failure behavior, or secret values |
| Historical GATE-06 production record | [Workflow run 34325228788](https://github.com/howardhayden/folio/actions/runs/34325228788), `commit 9ab26b95cc1f9a94697118c0fc20a849db8f6ad2`; Safari capture at `2026-09-09T08:05:50Z`, SHA-256 `266db6b8264a0aa42ac16916ddf696554c846b239960e7d19fc002917d843950`; Brave captures during `2026-09-09T08:15:12Z` through `2026-09-09T08:22:30Z`, SHA-256 values `8cd8a22b1b8375d05d7f1f727dafbd7fd604ed91dec3d8cee853fb071f8cb957`, `d9b84b3f5a49b383d2472ffdb909dfe0bd68fb822bdd5d64ae05421654092741`, `247777816b443b6eb9e4b63012941cbb38b342108347b081f99f457412ff17dd`, and `5f14ff23d8b2749dd81830656b9d71f0d58e79f853121f52860d28b62e4c1c64`; owner-attested capture-wide unique-marker search returned `0 matches` | Composite first-session canonical-client lifecycle across Safari/WebKit and Brave/Chromium; both origins; `200` acquisition, `204` release and zero-byte Safari response; model-asset boundary; no submitted marker or RUM emission in the reviewed trace | A terminal conversion, independent Payload-pane inspection, renewal, human verification, anti-bot assurance, universal browsers, disabled Cloudflare Web Analytics, absence of ordinary provider metadata, or raw secret/header values |
| Response-policy Worker | `workers/text-to-lattice-response-policy/worker.js`, its checked-in Wrangler routes, and `scripts/verify-text-to-lattice-route-inventory.mjs` | Exact four-path policy, proxy behavior, absence of Worker-owned telemetry, fail-closed authenticated route-inventory method, and the implementation checked live in run 34320931448 | Cloudflare's blocked edge-injected Web Analytics tag or behavior after later deployment drift |
| Secret bootstrap and demonstration profile | `scripts/bootstrap-text-to-lattice-secrets.mjs` and `workers/text-to-lattice-lease/demonstrationProfile.js`; exact four-name encrypted-binding inventory and successful direct testing-token lifecycle in run 34320931448 | Preservation-first binding plan, independent signing-secret generation, exact provider testing pair, installed name boundary, and live Siteverify/lease/release integration | Secret values, browser or widget participation, or production anti-bot assurance |
| Browser runtime set | [WebLLM](https://github.com/mlc-ai/web-llm) 0.2.82; [Web Tokenizers](https://github.com/mlc-ai/tokenizers-cpp) 0.1.6; [MLC Web XGrammar](https://github.com/mlc-ai/xgrammar) 0.1.27 | Named runtime identities and source repositories | Final browser behavior or binary provenance |
| Qwen MLC model | [`a5c9fab855e3ccbdfed2e7e69683d75f30332161`](https://huggingface.co/mlc-ai/Qwen3-4B-q4f16_1-MLC/tree/a5c9fab855e3ccbdfed2e7e69683d75f30332161) | Immutable artifact-repository revision | Independent conversion provenance |
| Llama MLC model | [`1e80abf71e3d17cd564e2d2b63caa15cb226018e`](https://huggingface.co/mlc-ai/Llama-3.2-3B-Instruct-q4f16_1-MLC/tree/1e80abf71e3d17cd564e2d2b63caa15cb226018e) | Immutable artifact-repository revision | Complete MLC conversion and artifact-license chain |
| Qwen tokenizer | `tokenizer.json`; SHA-256 `aeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4`; SRI `sha256-rrEzB6cazY/oGGHZStVKtonfdzMYgJ7tPL55S0SS2uQ=` | Expected tokenizer bytes and runtime integrity identity | Model behavior or source authorship |
| Llama tokenizer | `tokenizer.json`; SHA-256 `79e3e522635f3171300913bb421464a87de6222182a0570b9b2ccba2a964b2b4`; SRI `sha256-eePlImNfMXEwCRO7QhRkqH3mIiGCoFcLmyzLoqlksrQ=` | Expected tokenizer bytes and runtime integrity identity | Model behavior or source authorship |
| WASM repository | [`025bcaf3780fa8254f5e5efd3bfea0a5397248f4`](https://github.com/mlc-ai/binary-mlc-llm-libs/tree/025bcaf3780fa8254f5e5efd3bfea0a5397248f4/web-llm-models/v0_2_80) | Immutable binary tree | License grant or reproducible build |
| Qwen WASM | `Qwen3-4B-q4f16_1-ctx4k_cs1k-webgpu.wasm`; 6,070,240 bytes; SHA-256 `5ddf44e49b03e53e24fd29a45591850924346140452f60c29280190388571340`; SRI `sha256-Xd9E5JsD5T4k/SmkVZGFCSQ0YUBFL2DCkoAZA4hXE0A=`; git blob `449f3615aa712b22e548f5ad91bb9fbee0c12259` | Exact library identity | Source equivalence |
| Llama WASM | `Llama-3.2-3B-Instruct-q4f16_1-ctx4k_cs1k-webgpu.wasm`; 6,131,270 bytes; SHA-256 `34de0d60ab598c6a85ae882b48474f250193076f902057a21070bb2daae96d5b`; SRI `sha256-NN4NYKtZjGqFrogrSEdPJQGTB2+QIFeiEHC7LarpbVs=`; git blob `beda4ea1d45dc6c5294972b3ea3edfd317462142` | Exact library identity | Source equivalence |
| Final WASM artifact commit | [`a973639103dd389292a0315274495cb1bc406c7c`](https://github.com/mlc-ai/binary-mlc-llm-libs/commit/a973639103dd389292a0315274495cb1bc406c7c) | Both current blobs were finalized after the recorded TVM revision and remain unchanged at the pinned repository revision | Exact checkout, clean tree, compiler, dependency graph, or build commands |
| WASM introduction | [binary-libs PR #158](https://github.com/mlc-ai/binary-mlc-llm-libs/pull/158), merge `6ed5b97c37f4cdc49d1a8044a339db5588176d7e` | Current blobs entered through the identified recompilation | Complete build recipe or independent verification |
| Named TVM source | [`c8515e1ddfaf4d1afff916c484e68e1513631dd6`](https://github.com/apache/tvm/commit/c8515e1ddfaf4d1afff916c484e68e1513631dd6) | Source revision asserted by PR #158 | Proof the published bytes were produced only from that tree |
| Named MLC-LLM source | [`4084e7fe7532fb932810d8b707f2d760eefd0dea`](https://github.com/mlc-ai/mlc-llm/commit/4084e7fe7532fb932810d8b707f2d760eefd0dea) | Source revision asserted by PR #158 | Complete dependency, flag, or compiler identity |
| Llama license | SHA-256 `8cc15535a8a34b41888f644b339a1a9eb428af793a4f5e24df58a3e5b1487d74` | Checked-in terms match the reviewed official file | Approval of this public use |
| Llama AUP | SHA-256 `40e2777d7faa6beaf98400654170f414d8ab29b921b5163ad4ea0a1d39894201` | Checked-in policy matches the reviewed official file | Enforcement by the product |
| Llama-use evaluation fixture | [`LLAMA-USE-EVALUATION-CASES.json`](LLAMA-USE-EVALUATION-CASES.json); SHA-256 `259ba4cc17d28e16bf9470621f95af017e26988e22602c452246f055e0b8d3b4`; five expected denials and five expected allowances | Stable non-operational cases and expected purpose-and-consequence dispositions | Actual behavior of the exact verifier artifact |

The reviewed Llama files are byte-identical to the official Llama 3.2 files introduced in Meta's [`8d29d93fa5700a60532e0061a02ffa89d0acd3fc`](https://github.com/meta-llama/llama-models/commit/8d29d93fa5700a60532e0061a02ffa89d0acd3fc) commit and exposed through Meta's current [license](https://developer.meta.com/ai/llama3_2/license/) and [acceptable-use](https://developer.meta.com/ai/llama3_2/use-policy/) pages. The repository already carries the required Llama notice and “Built with Llama” attribution. Nothing located in those terms requires case-by-case advance permission for an ordinary deployment below the license's stated scale threshold; the recorded owner direction is this project's release control, not a claim that Meta operates an approval queue.

## Llama-use qualification

### What is already sound

- The exact base-model license and acceptable-use text is retained with stable digests.
- The verifier model and revision are pinned.
- The required Llama attribution is present.
- A verifier `safety: false` disposition suppresses candidate release.

### Controls and residual evidence

1. **Model-assistance disclosure.** At the point of use, state that Qwen drafts locally and Llama 3.2 checks locally; both can miss altered, omitted, biased, or unsafe meaning; every result requires human review; and the service is built with Llama.
2. **Source-specific confirmation.** Reset the confirmation whenever source text changes. The visitor confirms that the use is lawful, authorized, within the supported language boundary, and does not materially further conduct prohibited by the Llama AUP.
3. **Purpose-and-consequence safety.** The prompt contract fails closed when the requested transformation or candidate materially furthers a prohibited outcome or when necessary authority cannot be established. It may handle quotation, history, criticism, journalism, fiction, prevention, or defensive analysis when the transformation does not further the prohibited outcome.
4. **No topic blacklist.** Do not substitute keyword matching for purpose, authority, and consequence. It predictably rejects legitimate contexts, misses paraphrases, and creates a brittle policy fork.
5. **Executable evidence.** The checked-in [`LLAMA-USE-EVALUATION-CASES.json`](LLAMA-USE-EVALUATION-CASES.json) fixture balances realistic operational harm, malware improvement, deliberate deception, unauthorized professional practice, and sensitive-person inference against journalism, history and criticism, fiction, prevention and defense, and rights analysis. It has not been executed and manually reviewed against the exact verifier artifact in this environment. That unperformed run is recorded, not implied.
6. **Artifact provenance.** The Llama MLC conversion chain remains separate from behavior. Base-model terms and a Hugging Face commit pin do not establish every conversion input or artifact-level notice.

The source implements items 1–4, defines the ten-case non-operational fixture, and carries a direct safety-withholding regression. GATE-04A accepts the unexecuted exact-model fixture as a moderate residual while stating that verifier false negatives remain unobserved and explicit-denial withholding depends on the model's classification. The bounded official surface is local and tool-free, requires source-specific confirmation, withholds errors and explicit denials, and requires human review. GATE-04B separately accepts the incomplete publisher conversion and artifact-notice narrative while binding the exact repository revision, tokenizer digest, runtime contract, controlling base-model terms, and attribution. Hashes do not cure the authorization gap. Either gate reopens on consequential observed behavior, artifact drift, or terms drift.

This is a bounded risk disposition, not a claim that the exact model was tested here or that the converted artifact has a complete custody chain.

## WASM provenance qualification

The current record establishes **identity** and meaningful but partial **lineage**:

- both filenames, byte counts, SHA-256 digests, and git blob identifiers are exact;
- both current blobs trace to the recompilation merged by [PR #158](https://github.com/mlc-ai/binary-mlc-llm-libs/pull/158);
- that pull request names TVM and MLC-LLM source commits; and
- those source trees carry Apache-2.0 licenses ([TVM](https://github.com/apache/tvm/blob/c8515e1ddfaf4d1afff916c484e68e1513631dd6/LICENSE), [MLC-LLM](https://github.com/mlc-ai/mlc-llm/blob/4084e7fe7532fb932810d8b707f2d760eefd0dea/LICENSE)).

The current record does **not** establish a reproducible artifact:

- the pinned binary repository publishes no standalone root license or notice;
- the [historical `compile_wasm.py` at the introducing merge](https://github.com/mlc-ai/binary-mlc-llm-libs/blob/6ed5b97c37f4cdc49d1a8044a339db5588176d7e/web-llm-models/v0_2_80/compile_wasm.py) uses placeholders and local paths, requires manually uncommented calls, and does not pin the complete compiler, environment, dependency graph, configuration, or commands; its `qwen3_4b` preset does not exist at the recorded MLC-LLM revision, so the published helper cannot produce the Qwen target as written; the later pinned repository revision deletes that helper;
- MLC-LLM's pinned `tokenizers-cpp` submodule at `55d53aa38dc8df7d9c8bd9ed50907e82ae83ce66` has no `Cargo.lock`, its CMake path invokes an ordinary network-resolving `cargo build`, and the Python dependency declarations contain open ranges rather than a build lock;
- the binaries do not embed a useful producer, source-map, version, or license section; observed strings include workstation paths, which are provenance clues rather than a recipe; and
- no independent rebuild has produced and matched the published bytes.

The license gap is not cured by organizational proximity. [GitHub's repository-licensing guidance](https://docs.github.com/articles/licensing-a-repository) explains that default copyright restrictions remain when a repository has no license, while the [GitHub Terms of Service](https://docs.github.com/site-policy/github-terms/github-terms-of-service) supply only the platform-facing rights needed to provide public-repository functionality. WebLLM 0.2.82 is Apache-2.0 and intentionally names the `v0_2_80` directory and these model-library filenames, which is strong evidence of intended integration. Its license does not expressly grant rights in a separately hosted repository whose own license metadata is null.

The TVM qualification is narrower than a simple commit citation, but the final chronology is plausible. An initial artifact commit, [`40ef9663ecf3474f2b03b4edcead0d362a283e41`](https://github.com/mlc-ai/binary-mlc-llm-libs/commit/40ef9663ecf3474f2b03b4edcead0d362a283e41), predates the PR's recorded TVM commit `c8515e1…`; the current blobs were then rewritten by [`a973639103dd389292a0315274495cb1bc406c7c`](https://github.com/mlc-ai/binary-mlc-llm-libs/commit/a973639103dd389292a0315274495cb1bc406c7c) after `c8515e1…` and remain unchanged at the pinned binary-repository revision. The earlier time-plausible TVM PR head [`372c92844ce708cb1c9e1d13a2102823a4e50498`](https://github.com/apache/tvm/commit/372c92844ce708cb1c9e1d13a2102823a4e50498) differs from `c8515e1…` only in two Torch-frontend files and one CUDA file, outside the inspected Web/runtime/model-library path. The final embedded allocation values also agree with the recorded MLC source's 1,024-token prefill × model hidden size × two-byte float calculation. Together these facts corroborate source plausibility; they do not prove the builder's checkout, clean-tree state, commands, or compiler. TVM's CI pins Emscripten 3.1.51 at that source, yet no evidence proves the local macOS builder used it.

Both files expose the same 13-import/93-export ABI shape and embed the expected model-family, `q4f16_1`, 4,096-token context, 1,024-token prefill-chunk, and 128-sequence batch metadata. They contain no custom producer, version, source-map, license, or notice section. These observations support compatibility and triage; they do not establish authorship, reproducibility, or license custody.

Two useful lifecycle-improvement paths remain:

- obtain an upstream artifact-level license and build record that binds the exact files to complete inputs, toolchain, commands, flags, notices, and digests; or
- produce controlled replacements from licensed pinned sources, record the complete dependency and compiler environment, publish notices, and verify the resulting bytes and behavior. A source-built replacement need not match the upstream binary, but its own provenance, integrity, compatibility, and tests must be complete.

Switching models does not erase the boundary by itself. It creates a new model, conversion, runtime, behavior, license, and regression qualification while leaving the general WASM provenance question intact.

### Controlled replacement option

A controlled replacement is feasible without trying to recreate an undocumented workstation byte for byte. The build should compile the two pinned `mlc-chat-config.json` inputs directly, because the historical Qwen preset is absent. It must point MLC-LLM explicitly at external TVM `c8515e1…`; MLC-LLM's own TVM submodule is a different revision. The reviewed config digests are `9726ac7dbcd90475f8045604be0862ef1b4837e432c5004da20b8a6e348330da` for Qwen and `10b2318d871320ca66c86b7a44cb444cea7677e078a1b147d4e0646958a87dbe` for Llama.

If a controlled replacement is pursued, its acceptance should use one bounded, reviewable evidence package:

1. An immutable Linux builder image with compiler identities, exact source/submodule/input manifests, and both builder and distributed-code SBOMs.
2. Hash-locked Python inputs and vendored Cargo crates; the build must succeed with locked, offline dependency resolution.
3. A complete environment and command transcript, including stable source paths and explicit TVM/MLC configuration.
4. Two independent clean builds that produce byte-identical replacement outputs, followed by digest, ABI, embedded-configuration, and import/export allowlist checks.
5. A link-derived license and notice inventory for material actually shipped in each WASM, kept distinct from the broader builder inventory.
6. Signed SLSA or in-toto provenance, project-controlled immutable hosting, and integrity enforcement for the exact public URLs.
7. WebLLM 0.2.82 first-load, cached-load, structured-output, tamper-failure, and production-network tests against both pinned model repositories.
8. Human review and an exact-revision owner disposition. Llama behavior and model-artifact authorization remain separate gates.

The replacement acceptance criterion would be reproducible project-controlled output with a complete custody record and compatible behavior—not equality with undocumented upstream bytes. Reverse-engineering every theoretical component of the stripped binaries would add cost without reliable closure and is negative marginal value for the current product boundary. GATE-04C therefore records an accepted moderate residual with two distinct parts: exact identity, available lineage, ABI, embedded configuration, immutable runtime URLs, release-time byte verification, protected-request SRI, and protected-cache digest verification reduce technical substitution and incompatibility risk; reproducibility and artifact-level authorization and notice custody remain explicitly unestablished and are not reduced by those hashes. The accepted boundary is a browser-local demonstrator retrieving the exact publisher-hosted libraries, not redistribution of those libraries from hah.dev.

## Gate disposition

| Gate | Status | Marginal value | Release consequence |
| --- | --- | --- | --- |
| GATE-01 · Exact release artifact | Release workflow enforced | High | A stale or substituted artifact could escape the reviewed boundary; the workflow fails closed and binds current main. |
| GATE-02 · Deployed origin, secret, and demonstration boundary | Satisfied in production | High | Run 34320931448 verified the exact origins, routes, policies, frame bytes and headers, four-name encrypted-binding inventory, fail-closed boundaries, official demonstration site key, and direct dummy-token `200` acquisition plus `204` cleanup. The direct result remains server evidence, not browser, widget, human-verification, privacy-trace, or anti-bot evidence. |
| GATE-03 · Edge and provider capacity controls | Post deployment verification | Moderate | Source controls bound cost, state, and privacy consequence rather than availability. One client can fill and renew all eight slots with the reusable dummy token, and the 48-per-10-second edge rule does not stop its permitted 16-request acquisition sequence; record that rule as defense in depth, measure live behavior, and requalify on observed abuse. |
| GATE-04A · Llama behavior and public-use controls | Accepted residual risk | Moderate | Source disclosure, confirmation, semantic safety, candidate withholding, and balanced fixtures are present; exact-model execution is not claimed. |
| GATE-04B · Llama MLC-artifact provenance | Accepted residual risk | Moderate | Immutable identity and controlling terms are bound; the publisher conversion and artifact-notice narrative remains incomplete. |
| GATE-04C · WASM provenance and license boundary | Accepted residual risk | Moderate | Exact identity, lineage, ABI, and configuration are bound; reproducibility and artifact-level notice custody remain unestablished. |
| GATE-05 · Manual interaction and accessibility | Accepted residual risk | Moderate | Automated and browser checks cover critical paths; the eventual deployed assistive-technology matrix remains follow-up evidence. |
| GATE-06 · Production lifecycle and privacy trace | Post deployment verification | High | The earlier first-session record remains historical lifecycle and privacy evidence for its deployed revision. The repaired emitted worker now passes a Window-free final-artifact smoke, but the deployed official page must still record one supported-browser non-error terminal conversion and repeat the 200 acquisition, 204 release, and sanitized two-origin privacy trace. The blocked Cloudflare beacon remains a non-content-bearing edge-injection residual; testing-profile success does not establish anti-bot assurance, and consequential failure returns the client immediately to held documentation-only publication. |

Owner direction is recorded for the qualified source set, and independent production workflow evidence continues to satisfy GATE-02. GATE-06 is post-deployment verification for the repaired model-worker runtime: its historical browser record remains typed, the emitted bundle passes its new Worker-realm smoke, and one supported-browser non-error terminal conversion plus the repeated lifecycle and privacy trace remain due after deployment. No gate is open-release-blocker, so the repaired source candidate remains **qualified and enabled**, but it is not yet described as operationally complete. Capacity, broader exact-model-fixture execution, complete assistive-technology coverage, provenance, and reproducibility remain separately typed residual or follow-up boundaries.

## Deployment freshness and retained runs

The Pages workflow resolves GitHub's live `refs/heads/main` and requires its exact commit to equal `GITHUB_SHA` at its publication boundaries. Missing credentials, an unexpected ref, an unreadable or malformed API response, and a stale workflow commit fail closed. Immutable action pins, machine release-state checks, the exact response-policy deployment, preservation-first secret bootstrap, service and secret-name checks, and the qualified source-set digest make the control reusable across later releases. Run 34320931448 supplies the retained production record that closed GATE-02 for commit `a9b1db818dc7bb87b5bee16d0e168bf17d130185`; run 34325228788 deployed commit `9ab26b95cc1f9a94697118c0fc20a849db8f6ad2` before the retained GATE-06 browser evidence was captured. An enabled release repeats the three-service deployment and live probes on every main-branch push. A held release attempts them only through the explicit `[deploy-text-to-lattice-services]` head-commit marker or an approved manual dispatch, so documentation publication remains decoupled from provider mutation and allowance when the client is held.

## Marginal-value decisions

| Finding | Classification | Disposition | Reason |
| --- | --- | --- | --- |
| Leading-zero `Retry-After` inflation | Moderate/high | Fixed; retain | A valid ordinary response could manufacture an excessive wait; the canonicalization is cheap and reusable. |
| Pre-1900 HTTP-date rejection | Low | Completed behavior retained | The provider-impossible case has bounded consequence; completed work is not rolled back. |
| Keyword-only prohibited-topic blocking | Negative | Do not implement | Bespoke complexity creates false positives and trivial evasions while reducing semantic fidelity. |
| Deterministic language detection as sole language gate | Negative | Do not implement | Short and mixed text makes a bespoke detector unreliable; a resettable confirmation is clearer and bounded. |
| Point-of-use disclosure and source-specific confirmation | High | Fix now | Ordinary visitors otherwise cannot understand model participation, limitations, or the authority boundary. |
| Machine-enforced release-state publication | High | Fixed; retain | It converts a prose warning into a reusable lifecycle control for every future revision, enabled this exact release only after production satisfaction, and keeps future drift fail-closed. |
| Full historical WASM byte-for-byte rebuild | Negative | Do not require for this boundary | Bespoke reconstruction of unavailable workstation state adds greater maintenance burden than plausible risk reduction after exact identity and compatibility controls. |
| Maintainable controlled WASM replacement | Moderate | Revisit when bounded | A project-controlled replacement can improve custody and notices if it is supportable without recreating undocumented history. |
| Response-policy enforcement despite the static GitHub Pages origin | High | Fixed in release boundary | Missing CSP and Permissions-Policy would create a realistic runtime-expansion and cross-origin failure. A small exact-path edge Worker is reusable and live-verifiable without changing the static origin. |
| Portfolio-wide response-policy Worker route | Negative | Do not implement | Attaching `hah.dev/*` would consume Worker allowance for unrelated portfolio traffic and expand the blast radius of a Text to Lattice-specific failure. Four exact document routes plus the in-document CSP fallback protect the authorized launch surfaces with lower lifecycle cost. |
| Cloudflare official testing credentials for the demonstrable release | Moderate | Bounded demonstration only | The canonical browser flow can traverse the frame, widget, Siteverify, lease, and release path; a direct dummy-token success proves only the last three. Other origin, privacy, signing, admission, lease-cap, and expiry controls bound financial, privacy, and state consequence—not availability. The profile must not be represented as anti-bot assurance. |
| Public testing-token slot starvation and the 48-per-10-second edge rule | Moderate | Accept residual; harden and monitor | One client can use the allowed 16-request sequence to fill and renew all eight slots; the per-IP rule admits that sequence. Exact caps and fail-closed free-tier behavior limit consequence to availability, which the interface does not assure. Apply the rule when feasible and requalify on observed abuse. |
| Bespoke attestation bypass route or unsigned token mode | Negative | Do not implement | A second public protocol would add attack surface and maintenance burden. The exact provider-published testing profile is narrower, explicit, replaceable, and remains inside the existing Siteverify and signed-lease contract, while explicitly not proving widget participation. |
| Automatic preservation-first secret bootstrap | High | Fixed in release boundary | Main-branch deployment otherwise stalls on absent bindings or risks ad hoc value handling. Exact-name inventory, independent random signing values, complete-pair preservation, and fail-closed mixed-state rejection create a reusable lifecycle control without printing secrets. |
| Immediate post-deployment official-page acquisition, terminal conversion, release, and privacy trace | High | Repair implemented; repeat after deployment and return to held on failure | The dated Safari/WebKit and Brave/Chromium records remain historical lifecycle and privacy evidence for their deployed revision, but the Worker failure meant they did not establish a terminal conversion. After the repair deploys, repeat the supported-browser lifecycle and privacy review and bind one non-error terminal result to that deployed revision. Failed acquisition, conversion, or release, sensitive prose leaving the browser, or a later consequential trace defect remains a rollback trigger. Demonstration-profile success remains explicitly narrower than real-key anti-bot evidence. |
| Deliberately timed real-browser renewal trace | Moderate | Observe naturally; defer as release gate | A real renewal defect could affect a long session, but source and adversarial tests cover timing, bodyless PATCH behavior, fail-closed termination, bounded expiry, and eventual cleanup. Record renewal when ordinary use reaches the interval and roll back on an observed defect; a deliberate wait or bespoke harness adds little incremental assurance. |
| Held résumé title and Text to Lattice icon link drift | Moderate | Fixed; regression tested | A stale built-boundary assertion could reject the corrected canonical-title and SVG-only tool-link contract; the plausible mismatch was cheap to repair without weakening the hold. |
| Shared verification-frame propagation deadline | Moderate | Fixed; regression tested | A slowly settling first asset could leave later healthy frame assets only a token verification window; separate bounded windows remove a recoverable deployment false negative. |
| Pinned Workerd redirect incompatibility in the Siteverify subrequest | High | Fixed; regression tested | The deployed runtime rejected `redirect: "error"` before Siteverify ran, so every realistic acquisition failed with HTTP 403. Manual redirect handling restores interoperability while every 3xx remains rejected and cancelled. |
| Replay of an ambiguous live lease acquisition probe | Moderate | Fixed; regression tested | Retrying a non-idempotent grant after a transient result could consume another lease and obscure qualification. The live verifier now makes one bounded acquisition attempt while retaining retries for idempotent probes. |
| Simultaneous programmatic résumé-modal body-class race | Low | Document; defer | Normal interface paths cannot open two résumé modals at once; reference-counting the class would add coordination complexity for a nonconsequential programmatic-only state. |

The rule is not “ignore edge cases.” It is: repair when consequence × plausibility × lifecycle value justifies the change. Novel findings remain eligible; nonconsequential novelty does not acquire release priority merely by being difficult.

## Requalification boundary

Re-run this decision after any model, tokenizer, runtime, WASM, Llama-terms, prompt, safety schema, candidate-release, origin, response-policy, credential profile, provider, lease, storage, telemetry, accessibility-critical, qualified-source, or artifact-digest change. The current owner direction binds the named qualified source set; production run 34320931448 supplies GATE-02 evidence for its named deployed commit; and the browser evidence captured after run 34325228788 remains historical GATE-06 lifecycle and privacy evidence for that deployed revision. After the worker-realm repair deploys, record one supported-browser non-error terminal conversion and repeat the supported-engine lifecycle and sanitized two-origin privacy trace. Repeat that functional evidence after any consequential widget, key, hostname, action, lease-protocol, origin, provider, telemetry, runtime, or request-contract change. The interactive demonstrator remains publishable while no gate is open-release-blocker; any GATE-02 drift or consequential GATE-06 failure returns it to held documentation-only publication.
