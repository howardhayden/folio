# Text to Lattice resume demonstrator requirements

This file records the public contract for the Lattice project card and its capability-scoped **Text to Lattice** demonstrator. The browser may connect only to the same-origin `/api/lattice` capability; that Worker performs the declared external model processing. This file does not restate or redistribute the Relational Systems Register profile.

## Sources

- **U1:** Site-owner brief, 2026-09-01: add the Lattice project, supplied summary, icon, documentation link, pop-up interaction, and initial 250-character limit.
- **U2:** Site-owner correction, 2026-09-01: raise the limit to 700 words, require material rewriting unless the input already clears the bounded checks, and select content layers below the whole-document level without defaulting to experiential.
- **U3:** Site-owner correction, 2026-09-01: accept realistic dialogue-heavy narrative input, perform material work instead of discarding every local candidate, move beyond a fixed experiential treatment, and represent processing time only when real work requires it.
- **U4:** Site-owner correction, 2026-09-08: use the display name Text to Lattice, remove its dedicated Close button while retaining the résumé pop-up dismissal contract, share the Shelf search-entry treatment with the larger text area, and protect independent API signing domains with encrypted secrets.
- **U5:** Site-owner correction, 2026-09-08: remove the input-edge shadow; move hardening, API, and Hugging Face details out of the modal; keep visible copy concise and Latticed; harden source and clarification handling; deter output extraction and capture without overstating browser control; omit extraction guidance from the interface; and show a retry ETA when the quota API returns a limit.
- **U6:** Site-owner correction, 2026-09-08: make the public API limiter sufficient for concurrent free-tier demonstration use, preserve bounded retry ETAs, and keep every protection-bearing value in encrypted provider or repository secrets.
- **U7:** Site-owner documentation brief, 2026-09-08: publish Lattice-register interactive Markdown documentation for the Lattice method and its Text to Lattice wrapper; include a concept and ecosystem map, an NN/g-derived system skill map, a service blueprint, and security modeling; link every artifact directly from the résumé’s Lattice project surfaces; and add CHORUS Concept Map and CSD Matrix links with the Documentation icon.
- **U8:** Site-owner release-qualification directive, 2026-09-09: verify, document, and remediate the Llama-use and unresolved WASM-provenance gates as intensely as possible; judge findings by consequence × plausibility × lifecycle value rather than novelty; preserve completed work; and do not publish an interactive client whose exact evidence remains open.
- **U9:** Site-owner navigation correction, 2026-09-09: give every project-resource link enough information scent to identify both its project or wrapper scope and the artifact it opens; do not use a vague “Documentation” label.
- **U10:** Site-owner release correction, 2026-09-09: finish GATE-02 and make Text to Lattice available as an honestly labeled demonstrable release; bypass unavailable real-browser renewal evidence when its incremental assurance does not justify holding the release, but retain rollback on any naturally observed renewal failure.
- **U11:** Site-owner runtime and continuity correction, 2026-09-11: replace the misleading full preparation bar with truthful download, final-setup, and inference phases; bound stalled local work; preserve the same input, availability or attestation request, lease-bound conversion, post-request processing, clarification, error, and result whenever the modal is dismissed and reopened while the résumé remains mounted; make Cancel and Start over terminate or reset the current request and make component unmount tear it down while keeping terminal results and errors reopenable; restore a visible Close action; and reject conditionally injected analytics markers without weakening the CSP or claiming that provider analytics is disabled.
- **U12:** Site-owner capability-scoped privacy correction, 2026-09-14: replace active browser-local inference with one explicit same-origin `POST /api/lattice`; send exactly `{text, requested_mode, schema_version: 1}` only after external-processing confirmation; keep Qwen3-4B as the fixed server-side generator and Llama 3.2 3B Instruct as the fixed server-side verifier through the configured Hugging Face/Featherless service; prohibit automatic provider or model fallback; retain no content in hah.dev application storage, raw logs, caches, queues, or analytics; warn against restricted information; bound every terminal path; and do not claim byte equivalence between provider serving and the historical MLC artifacts. Earlier runtime and lease bullets above are chronological provenance, not the active contract where U12 supersedes them.
- **L1:** `howardhayden/lattice` v0.1.1 README, architecture, requirements, context types, validators, and license scope at commit `d6cc85b275e3f14163a5a547f626832fd21b27b0`.
- **F1:** Existing `hah.dev` resume project-card, progressive-disclosure, resource-link, and modal patterns.

## Atomic requirements and evidence

| ID | Requirement | Source | Evidence |
| --- | --- | --- | --- |
| LAT-UI-001 | Add Lattice to Resume → Projects. | U1 | `ResumeProjects.tsx` project data; SSR tests |
| LAT-UI-002 | Use Bootstrap’s `pen-fill` path and the existing project-icon treatment. | U1, F1 | `PenFillIcon`; SSR tests |
| LAT-COPY-001 | Render the supplied first paragraph openly and conceal the remaining three under “Read me.” | U1, F1 | `.project-readme` disclosure; SSR tests |
| LAT-LINK-001 | Link the Lattice source repository at `https://github.com/howardhayden/lattice` using the existing resource formatting and the descriptive label “Lattice Source Repository.” | U1, U9, F1 | project resource data; SSR tests |
| LAT-DOC-001 | Keep one versioned machine-readable authority for four separately bounded artifacts: the Lattice concept and ecosystem map, Lattice system skill map, Text to Lattice service blueprint, and Text to Lattice security model. Do not reproduce the protected register-rule inventory. | U7, L1 | documentation atlas; generator validation |
| LAT-DOC-002 | Generate complete Markdown and self-contained progressively enhanced HTML from the same authority. HTML must retain substantive meaning without JavaScript; interaction may filter, reveal, and export a nonauthoritative Markdown view but may not rewrite classifications or claims. | U7, F1 | deterministic builder; Markdown/HTML parity and drift tests |
| LAT-DOC-003 | Adapt NN/g skill mapping to documented system capability, never to the author’s proficiency. Tie every score to repository evidence, reserve external-validation status for independent production, user, or assistive-technology evidence, and show open evidence separately. | U7 | skill-map scale, exact table, and claim boundary |
| LAT-DOC-004 | Keep the Lattice engine and the browser wrapper distinct. The method map must state the typed-contract guarantee and its host/reviewer boundaries; the wrapper documents must trace visible, local, network, evidence, ownership, threat, pre-publication, and residual boundaries without presenting source tests as deployed proof. | U7, L1 | concept ledger; blueprint; security register |
| LAT-DOC-005 | Publish stable direct HTML and Markdown paths with canonical, alternate, manifest, and SHA-256 records; fail the normal build when generated artifacts drift from their authority. | U7 | generated index and artifact manifests; `docs:lattice:check` |
| LAT-LINK-002 | Expose direct links to all four Lattice artifacts in the interactive résumé, `/resume/`, the canonical Lattice and tool pages, project/resume manifests, JSON-LD, canonical Markdown, sitemap, and llms discovery records. Name every link with its Lattice-method or Text-to-Lattice-wrapper scope and exact artifact type. | U7, U9, F1 | shared document registry; rendered and semantic tests |
| CHR-LINK-001 | Add CHORUS Concept Map and CSD Matrix links to its résumé project card at their canonical `chorus.observer/documentation/` HTML paths; label the notebooks destination “CHORUS Notebooks”; and use `backpack4` with external-link semantics for all three. | U7, U9, F1 | project resource data; rendered-link and icon tests |
| TTL-NAME-001 | Name the interactive tool exactly “Text to Lattice”; keep hyphenation only in technical identifiers and routes. | U4 | dialog heading; canonical semantic contract; source-contract tests |
| TTL-UI-001 | Open Text to Lattice in the resume’s established labelled, focus-managed pop-up treatment; dismiss it through its visible Close action, backdrop, or Escape. While the résumé remains mounted, ordinary dismissal is visibility-only in every phase and preserves the same request and state for reopening. Cancel and Start over terminate or reset the current request, and component unmount tears it down; a terminal result or error remains reopenable. | U1, U4, U11, F1 | `.modal.resume-modal.lattice-modal`; dialog tests |
| TTL-UI-002 | Style the text area as an enlarged Shelf search entry: borderless, rounded, and without an edge shadow. Use a label and background change for visible keyboard focus. | U4, U5, F1 | `.shelf-search-entry.lattice-input`; source-contract tests |
| TTL-UI-003 | Keep pre-form copy to the action, essential limits, one canonical documentation path, and the explicit statement that text leaves hah.dev for external processing. Warn against classified, controlled, privileged, export-controlled, operationally sensitive, or otherwise restricted information; require the deliberate “Process with external service” action or Cancel. | U5, U12 | dialog source and rendered-copy tests |
| TTL-LIM-001 | Accept between 1 and 700 Unicode-aware words, inclusively, and reject 701 words without truncation. | U2 | shared word counter and engine guard; 700/701 tests |
| TTL-LIM-002 | Reject source above 12,000 UTF-16 code units and reject a request body above 65,536 bytes without truncation. The active remote schema has no clarification field or follow-up answer path. | U5, U12, L1 | shared browser/server guards; exact-boundary and request-envelope tests |
| TTL-LIM-003 | Reject pathological Unicode, more than 64 passages, more than 32 initial model work groups, or a run requiring more than 512 protocol completions before it can consume unbounded server or provider work. Permit retry-driven subdivision only within a separate ceiling of 64 executed work groups, while the shorter API and provider deadlines remain binding. | U5, U12, L1 | preflight, execution-group, provider-adapter, and adversarial tests |
| TTL-LAY-001 | Select the operative, experiential, or interpretive content layer for each sentence or bounded clause; report mixed-layer input when applicable. | U2, L1 | segment analysis and layer metadata; mixed-layer test |
| TTL-LAY-002 | Do not use experiential as a generic fallback. Leave insufficiently evidenced content unresolved, or inherit a single evidenced layer only within its local paragraph. | U2, L1 | scoring thresholds and local inheritance; classification tests |
| TTL-LAY-003 | Accept only `auto`, `operative`, or `experiential` as `requested_mode`. Treat operative and experiential as evidence-bounded preferences; semantic fidelity, safety, accessibility, and a better-supported layer override the preference. | U12, L1 | request validator, prompt contract, and mode-propagation tests |
| TTL-NAR-001 | Detect narrative as a document kind independently of operative, experiential, and interpretive passage layers; use actor-action and dialogue-attribution evidence without forcing every passage into the experiential layer. | U3 | narrative mode and quote-neutral local scoring; narrative tests |
| TTL-NAR-002 | Preserve every protected dialogue span as the exact UTF-16 code-unit sequence ingested by the browser (after HTML textarea newline normalization), and in order. Dialogue may inform narrative context, but quoted commands, modality, causal language, and general terms must not be treated as host instructions or document-wide defects. | U3, L1 | typed protected spans, neutral classification view, ordered marker gate; quotation tests |
| TTL-XFM-001 | A successful transformation of nonconformant text must change its normalized word sequence; capitalization, punctuation, or paragraph spacing alone is insufficient. Return that fully checked result as `translated`. | U2 | candidate materiality gate; interpretive and experiential tests |
| TTL-XFM-002 | Return the source text unchanged only with the explicit `conformant-for-context` outcome after the bounded checks find no actionable issue. | U2, L1 | result state; fixed-point and known-good tests |
| TTL-XFM-003 | When the pipeline cannot expose either a conformant source or a candidate that clears its required release gates, return `unable-to-attempt` and no result text; never relabel the source or a withheld candidate as a successful conversion. | U2, U3, L1 | result state and findings; vague-input and candidate-withholding tests |
| TTL-XFM-004 | When a material candidate clears the semantic release gates but retains a bounded lower-priority review issue, return the transformed document as `review-required`; distinguish it from complete bounded clearance. | U3, L1 | `review-required`; review-draft tests |
| TTL-SEM-001 | Preserve explicit actors, actions, objects, polarity, modality, uncertainty, quantities, units, conditions, exceptions, timing, causality, consequences, and recovery relationships. Invent none. | L1 | semantic sentinels and rule-local guards; preservation tests |
| TTL-PRO-001 | Keep direct safety, medical, contact, timing, recovery, and accessibility instructions literal in their local protected spans, including within mixed input. | L1 | protected-segment precedence; exact-substring tests |
| TTL-DET-001 | Keep host validation, ordering, schemas, seed, and failure projection deterministic. Do not promise byte-identical model output across provider revisions or claim that the active provider runtime reproduces historical local MLC execution. A partial candidate must never be relabeled as fully aligned while review notes remain. | U2, U3, U12, L1 | deterministic validators and failure projection; runtime-disclosure checks |
| TTL-LIC-001 | Do not embed or redistribute the exclusive register profile, its rule inventory, or its fixtures in public client code. | L1 | independently authored bounded host rules; source review |
| TTL-LIC-002 | Treat Llama acceptable use as a purpose-and-consequence boundary, separate from artifact provenance. Require a source-specific lawful, authorized, supported-language confirmation; disclose model assistance and known limitations at point of use; fail closed when the transformation materially furthers prohibited conduct or necessary authority cannot be established; and do not replace this judgment with a topic-keyword blacklist. | U8 | prompt and interface contracts; candidate withholding; adversarial release evidence |
| TTL-SUP-001 | Preserve the two historical WebGPU/MLC WASM identity records—filenames, byte counts, SHA-256 and SRI digests, git blobs, source revisions, and qualification history—as explicitly inactive provenance. They do not identify the bytes served by the current provider. | U8, U12 | historical model contract, release register, and qualification dossier |
| TTL-SUP-002 | State that the active Hugging Face/Featherless runtime may differ from the historical MLC artifacts in weights, tokenizer behavior, kernels, numerical behavior, and serving configuration. Claim no byte-for-byte equivalence or reproducibility bridge between them. | U8, U10, U12 | active implementation contract and historical-runtime disclosure |
| TTL-REL-001 | Keep the completed interaction dormant and absent from public executable bundles while any release gate is open or exact owner disposition is pending. Publish its canonical contract and documentation through native links. A machine validator must reject both a held bundle containing runtime endpoints/assets and an enabled register containing an open, malformed, or unevidenced gate. | U8 | release register; source/build validator; held-artifact tests |
| TTL-A11Y-001 | Provide a labelled dialog and textarea, shared word counter, inline error, short live status, visible Close action, backdrop and Escape dismissal, focus containment, and focus return. Do not mark the potentially 700-word result as one atomic live announcement. | U2, U4, U11, F1 | dialog markup and focus lifecycle; source-contract tests |
| TTL-UX-002 | Report the finite `validating`, `submitting`, `processing`, `success`, and `error` lifecycle honestly. Bound each provider call to 60 seconds, the complete Worker request and client wait to 240 seconds, and all response bodies by byte limits; surface terminal failures without simulated model-download or GPU phases. | U3, U5, U11, U12, F1 | remote client state tests; Worker and provider deadline tests |
| TTL-UX-004 | Treat Close, backdrop, and Escape as reversible visibility transitions while the résumé remains mounted: preserve input, submitted-request state, error, and result for reopening. Cancel aborts the current client wait; Start over resets it; component unmount tears it down. Terminal state remains reopenable, and no hidden retry or fallback begins after dismissal. | U11, U12, F1 | modal lifecycle and source-contract tests |
| TTL-UX-003 | When `/api/lattice` returns a valid bounded `Retry-After`, expose the wait accurately, but require another deliberate submission after it expires; never retry automatically. | U5, U12 | bounded API response parser and error-state tests |
| TTL-SEC-001 | Treat submitted text as inert data and render source, findings, and output through React text nodes, never HTML injection. | L1 | component rendering; injection tests |
| TTL-SEC-002 | Keep `HF_TOKEN` only as a server-side encrypted Worker secret; never expose it in client bundles, responses, source, application logs, or analytics. Keep deployment credentials in protected repository or deployment-environment secrets. | U4, U6, U12 | Worker configuration, secret-name, bundle, and response tests |
| TTL-SEC-003 | Run complete source preflight in the browser before submission and again in the Worker before provider work. Separate model instructions into system messages, place source only in delimited inert JSON data, require closed JSON Schema output, and deterministically verify every stage response. | U5, U12, L1 | prompt builders, dual preflight, schema, and preservation tests |
| TTL-SEC-004 | Restrict server egress to `https://router.huggingface.co/v1/chat/completions` with generator `Qwen/Qwen3-4B:featherless-ai` and verifier `meta-llama/Llama-3.2-3B-Instruct:featherless-ai`. User content must never determine a URL, provider, model, header, or credential; no provider or model fallback is permitted. | U5, U12 | provider adapter allowlist and instrumented-fetch tests |
| TTL-SEC-005 | Disable routine selection, copy, cut, drag, context-menu, touch-callout, and printing of generated output; conceal it on page invisibility, window blur, print, and detectable Print Screen events; add a reduced-motion-compatible OCR-deterring veil. Preserve semantic access and document that browser/OS capture and AI unreadability cannot be guaranteed. | U5 | component events, print/visibility CSS, canonical limitation |
| TTL-SEC-006 | Accept only the exact no-query `POST https://hah.dev/api/lattice` with `application/json`; return 404 for another path, 405 with `Allow: POST` for another method, 415 for unsupported media, and 400 or 413 for malformed, unknown-field, invalid-mode, version, Unicode, or size failures before provider work. Bound and validate every response. | U5, U12 | Worker request-order, exact-route, schema, and response tests |
| TTL-SEC-007 | Send exactly `{text, requested_mode, schema_version: 1}` and no ambient fields. Do so only after the user sees the external-processing and restricted-information warning and explicitly chooses “Process with external service.” | U12 | request constructor, explicit-action, zero-request-before-submit, and UI-copy tests |
| TTL-SEC-008 | Define no application database, object storage, cache, background queue, raw-content log, or analytics event for source, prompts, candidates, results, or raw provider bodies. Hold content only in bounded request memory and return `Cache-Control: no-store`. Do not extend this hah.dev guarantee to external-provider systems. | U12 | static sink scan, Worker bindings, response headers, and provider disclosure |
| TTL-SEC-009 | Use finite request, provider, output-token, response-size, retry, repair, and completion bounds. The transport and browser perform no automatic retry; schema correction may call the same fixed model within the pipeline budget but never switch provider or model. A terminal failure requires a new deliberate submission. | U12 | deadline, capacity, retry-count, and fixed-target tests |
| TTL-SEC-010 | Set browser `connect-src` to `'self'`; include no model-provider origin in client code or policy. Route all provider calls through the named server-side capability, and deny undeclared browser destinations. | U12 | CSP, capability manifest, and forbidden-origin tests |
| TTL-SEC-011 | Test forbidden origins, alternate paths and methods, query smuggling, extra or missing fields, modes and versions, 700/701-word boundaries, malformed/oversized/HTML provider responses, timeout, rate limit, no automatic retry, no fallback, and absence of service-worker or cache interception for `/api/lattice`. | U12 | adversarial capability and Worker test suites; CI manifest |
| TTL-SEC-016 | Return `Cache-Control: no-store` on API responses and keep the protected documents free of content-bearing analytics beacons or conditional injection. Treat source checks as pending live-provider evidence, not proof about external provider logging or retention. | U11, U12 | response-policy Worker, API headers, browser-shaped live verifier, and conditional-injection tests |
| TTL-SEC-012 | Disable clarification in the remote capability. Analyzer and reanalysis schemas require `questions: []`; preserve unresolved ambiguity or uncertainty as source-grounded semantic data while forcing a definitive best-supported classification. | U5, U12, L1 | closed analysis schema, mode prompt, and no-clarification result tests |
| TTL-SEC-013 | Permit only bounded protocol correction and repair calls to the same fixed role model. Never carry clarification history or route a failed stage to a different provider or model. | U5, U12, L1 | adapter call-target and bounded-attempt tests |
| TTL-SEC-014 | Verifiers cannot emit `clarify` or public questions. Post-candidate re-atomization receives only closed, host-safe failure fields, never rejected-candidate claims or provider-authored issue prose. | U5, U12, L1 | verifier/reanalysis schemas; semantic-laundering and candidate-sentinel tests |
| TTL-SEC-015 | When a candidate is withheld, expose no model atom identifiers, candidate-derived layer summary, passage-change counts, pass counts, or candidate-dependent finding categories; return one host-authored failure record. | U5, L1 | public-result projection; recursive noninterference tests |
| TTL-DEP-001 | Apply a main-document CSP whose `connect-src` is exactly `'self'`, retain restrictive default, frame-ancestor, object, base, and form policies, and launch the tool canonically at `/resume/#text-to-lattice`. Require the exact `https://hah.dev` browser origin before `POST /api/lattice`; local and preview origins must not proxy or bypass the production boundary. | U10, U11, U12, F1 | response-policy Worker; canonical-origin zero-fetch, deployment, and rendered-link tests |
| TTL-REL-002 | Before activation, verify a sanitized live trace showing the browser's single same-origin `POST /api/lattice`, the Worker's one fixed Hugging Face router destination, no provider origin in browser traffic, no content-bearing analytics or storage sink, correct no-store headers, bounded terminal errors, and rollback on mandatory failure. Do not record raw source or result as evidence. | U10, U12 | release register, operator runbook, and live capability verifier |

## Word-count policy

The counter and engine use the same locale-neutral `Intl.Segmenter("und", { granularity: "word" })` policy and count only `isWordLike` segments. A display-only Unicode fallback can show a count before capability resolution, but conversion fails closed when `Intl.Segmenter` is unavailable. Contractions such as “can’t” normally count as one word; Unicode segmentation may count a hyphenated compound as multiple word-like segments. The displayed count is therefore the engine’s count on a supported runtime, not a separate approximation.

The 12,000-code-unit ceiling and structural bounds are defensive implementation limits, not additional authoring allowances: input must clear them and the 700-word limit before the browser submits `/api/lattice`. The Worker repeats the checks before provider work.

## Outcome and preservation boundary

Text to Lattice is an independently authored pipeline behind one named remote capability. Its order is:

1. validate Unicode, word, character, passage, and batch limits in the browser before any request;
2. after explicit confirmation, send exactly `{text, requested_mode, schema_version: 1}` to same-origin `POST /api/lattice`;
3. repeat preflight in the Worker before provider work and segment the complete source losslessly into bounded passages and batches;
4. have the fixed server-side Qwen3-4B generator atomize meaning, select an evidence-supported register, and draft a material candidate while preserving protected source spans;
5. use the fixed server-side Llama 3.2 3B Instruct verifier and deterministic validators to check coverage, fidelity, accessibility, clarity, domain correctness, register fit, and ornament;
6. re-atomize, repair, and recheck rejected work through the same fixed models within request, call, completion, token, and response budgets;
7. certify required multi-batch consistency against the complete source; and
8. return `translated`, `conformant-for-context`, `review-required`, or `unable-to-attempt`, or terminate with one machine-readable request error.

The public engine accepts arbitrary prose within its published bounds and performs its own atomization. It does not certify text as universally or permanently “perfectly Latticed.” `conformant-for-context` returns the unchanged source only after it clears the selected-context checks. `translated` returns a material candidate that cleared the bounded gates; `review-required` returns a semantically releasable material draft with a bounded lower-priority issue and is not a certification of semantic equivalence. The remote capability does not ask clarification questions; it must make a best-supported classification or fail closed. `unable-to-attempt` returns no result text.

The source is not returned as transformed text when no material candidate clears preservation. hah.dev keeps source and result only in bounded request and browser memory and does not write them to application storage, raw logs, caches, queues, analytics, public HTML, manifests, JSON-LD, Markdown, sitemap, or llms files. Hugging Face, Featherless AI, and their infrastructure process submitted content under their own policies. The active remote runtime is not asserted to be byte-for-byte equivalent to the historical MLC/WebGPU artifacts.

## Supersession history

| Earlier requirement | Status | Replaced by | Reason |
| --- | --- | --- | --- |
| LAT-INT-002: maximum 250 user-perceived characters | Superseded by U2 | TTL-LIM-001, TTL-LIM-002 | The owner raised the authoring limit to 700 words; a separate defensive ceiling remains. |
| LAT-INT-003: select one layer for the entire input | Superseded by U2 | TTL-LAY-001, TTL-LAY-002 | Mixed inputs require sentence- or clause-level selection, and experiential is not a fallback. |
| LAT-SEM-001: preserve every word and its order while changing cadence | Superseded by U2 | TTL-XFM-001–003, TTL-SEM-001 | The tool must materially rewrite when safe; punctuation-only or paragraph-only changes do not satisfy the interaction. |
| Global all-findings success veto in the initial U2 implementation | Superseded by U3 | TTL-XFM-003, TTL-XFM-004 | A concern in one untouched passage must not erase safe, material revisions elsewhere. |
| Broad fixed-point language covering partial candidates | Refined by U3 | TTL-DET-001, TTL-XFM-004 | A partial candidate must exhaust safe revisions without being relabeled as fully aligned while review notes remain. |
| Hyphenated public display name | Superseded by U4 | TTL-NAME-001 | The owner aligned the public name with the rest of hah.dev. |
| Removal of the dedicated Close control | Superseded by U11 | TTL-UI-001, TTL-UX-004 | The owner restored a discoverable dismissal action while separating dismissal from cancellation and teardown. |
| Browser-local WebGPU inference, bodyless quota lease, Turnstile frame, renewal, and release requirements from U4–U11 | Superseded by U12 for the active runtime; retained as historical provenance | TTL-UI-003, TTL-UX-002–004, TTL-SEC-002–014, TTL-DEP-001, TTL-REL-002 | The active client now has one explicit same-origin content-bearing capability and a fixed server-side provider/model boundary. Historical MLC identity records remain inactive evidence and do not imply remote byte equivalence. |
